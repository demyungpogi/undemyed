import { SPLASH_TIMEOUT } from '@/constants/AppConstants';
import SplashScreen from '@/screens/SplashScreen';
import { router } from 'expo-router';
import { useEffect } from 'react';

const Splash = () => {
    return <SplashScreen/>;
};

export default Splash;
